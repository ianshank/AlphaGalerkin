{
  "description": "Warn-only quality hooks: hardcoded-literal and secret-pattern scan on edited files. Fail-safe by design; promote to blocking only via the 'gating' tunable after the false-positive rate is measured.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/quality_scan.py\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
